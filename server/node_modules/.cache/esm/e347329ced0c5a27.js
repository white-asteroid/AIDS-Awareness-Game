let express,mongoose,cors,readdirSync;_ed0‍.w("express",[["default",["express"],function(v){express=v}]]);_ed0‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_ed0‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_ed0‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);



const morgan = require("morgan");
require("dotenv").config();

const app = express();

mongoose
    .connect(process.env.DATABASE, {
        useNewUrlParser: true, 
        // userFindAndModify:false,
        useUnifiedTopology: true, 
        // userCreateIndex:true,
    })
    .then(()=>console.log("DB connected kya baat hai into 2"))
    .catch((err)=>_ed0‍.g.console.log("jindagi ka naam hi error hai laale ",err));

    app.use(express.json({limit : "5mb"}));
    app.use(express.urlencoded({extended : "true"}));
    app.use(cors({origin:["http://localhost:3000"], }));

    // app.post("/api/login",(req,res)=>{
    //     console.log("Register endpoint -> ",req.body);
    // });
    readdirSync('./routes').map((r)=>{
        app.use("/api",require(`./routes/${r}`));
    });

    const port = process.env.PORT || 8000;
     
    app.listen(port, ()=> console.log(`Server is running bichi ${port}` ));

