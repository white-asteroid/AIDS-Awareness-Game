_ab2‍.x([["default",()=>_ab2‍.o]]);const mongoose= require('mongoose');

const UserSchema = new mongoose.Schema({
    name: {
        type: String,
        required: true
    },
    email: {
        type: String,
        trim: true,
        required: true, 
        unique: true,
    },
    password:{
        type: String,
        required: true, 
        min:6,
        max:64,
    },
    // avatar:{
    //     type: String
    // },
    date: {
        type: Date,
        default: Date.now
    },
    about: {},
    photo : String, 


},{ timestamp:true }
);
// module.exports= User = mongoose.model("user", UserSchema);
_ab2‍.d(mongoose.model("user", UserSchema));